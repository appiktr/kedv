import 'package:flutter/material.dart';

/// Uygulama renkleri
class AppColors {
  AppColors._();

  /// Ana renk - Mor (#9B3B7B)
  static const Color primary = Color(0xFF9B3B7B);

  /// İkincil renk - Açık mor (#994D73)
  static const Color secondary = Color(0xFF994D73);

  /// Arka plan rengi - Çok açık pembe (#FCF7FA)
  static const Color background = Color(0xFFFCF7FA);

  /// Input arka plan rengi - Açık pembe (#F2E8ED)
  static const Color inputBackground = Color(0xFFF2E8ED);

  /// Ana metin rengi - Koyu (#1C0D14)
  static const Color textPrimary = Color(0xFF1C0D14);

  /// İkincil metin rengi - Mor (#994D73)
  static const Color textSecondary = Color(0xFF994D73);

  /// Beyaz
  static const Color white = Color(0xFFFFFFFF);

  /// Hint rengi
  static const Color hint = Color(0xFF9E9E9E);

  /// Hata rengi
  static const Color error = Color(0xFFE53935);

  /// Başarı rengi
  static const Color success = Color(0xFF43A047);
}
